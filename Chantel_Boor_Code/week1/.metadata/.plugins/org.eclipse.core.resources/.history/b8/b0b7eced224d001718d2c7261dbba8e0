package com.rev.p3;

public class reverse 
{
	public static void main(String[] args) 
	{
		StringBuilder s = new StringBuilder ("to reverse");
		int l = s.length();
		
		for(int i =l;i>=0;i--)
		{
			s.append(s.charAt(l-1));
			s.deleteCharAt(l-1);
		}
	}
}
