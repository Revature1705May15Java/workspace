package reverse;

public class rev {
	
	public String StringRev(String s){
	
		String temp=s;
		s="";
		for(int i=s.length();i>0;i--){
			temp=s.substring(i-1, i);
			s+=s.concat(temp);
		}
		
		
		return s;
		
	}

}
