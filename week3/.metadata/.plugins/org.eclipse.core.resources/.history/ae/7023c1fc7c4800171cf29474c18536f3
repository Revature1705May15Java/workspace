package reverse;

public class rev {
	
	public String StringRev(String s){
		int i = s.length();
		String temp=s;
		for(int x =i;x<0;x--){
			System.out.println(temp.substring(x-1,x));
			s+=temp.substring(x-1,x);
		}
		temp=s;
		
		return s;
		
	}

}
