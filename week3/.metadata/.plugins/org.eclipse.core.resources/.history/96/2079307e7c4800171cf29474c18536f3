package reverse;

public class rev {
	
	public String StringRev(String s){
		int i = s.length();
		String temp="";
		for(int x =i;x<0;x--){
			if(x==s.length()){
			temp += s.substring(x, x);} 
			
			temp+=s.substring(x-1,x);
		}
		temp=s;
		
		return s;
		
	}

}
