
package com.google.challenges; 

public class Answer {   
	public static int numOfRoots=0;
    public static int[] answer(int area) {
    	int[] Solution=new int[numOfRoots];
    	try{
        
    	numberOfRoots(area);
    	double num2=area;
    	
    	for(int i=0;i<numOfRoots;i++){
        	boolean rootable=(Math.floor(Math.sqrt(num2))>1);
        	if(rootable){
        		double root=Math.floor(Math.sqrt(num2));
        		int rootint=(int)root;
        		System.out.println("rootable "+i);
        		//Solution[i]=rootint*rootint;
        		num2=num2-root*root;
        		continue;
        	}else{
        		System.out.println("unrootable "+i);
        		//Solution[i]=1;
        	}
        	}
    	numOfRoots=0;
    	}catch(ClassCastException e){
    		e.printStackTrace();
    		return null;
    	}catch(IllegalArgumentException e){
    		e.printStackTrace();
    		return null;
    	}catch(ArrayStoreException e){
    		e.printStackTrace();
    		return null;
    	}finally{
    	return Solution;
    		}
    	}
    	

    
    public static void numberOfRoots(int num){
    	double num2=num;
    	boolean rootable=(Math.floor(Math.sqrt(num2))>1);
    	if(rootable){
    		numOfRoots++;
    		numberOfRoots((int)Math.floor(Math.sqrt(num2)));
    	}else if(num>1){
    		numOfRoots++;
    		numberOfRoots(num-1);
    	}else if(num==1){
    		numOfRoots++;
    	}
    }
    public static void main(String[] args) {
		int[] Solution = answer(5);
	}
}