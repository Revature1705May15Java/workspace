import java.util.*;
class TestingGrounds{

	public static void main(String []argh)
	{
		Scanner s = new Scanner(System.in);
		int t = s.nextInt();
		String[] pair_left = new String[t];
		String[] pair_right = new String[t];
		
		for (int i = 0; i < t; i++) {
			pair_left[i] = s.next();
			pair_right[i] = s.next();
		}
		
		HashSet hs = new HashSet();
		
		for(int i = 0; i < t; i++){
			if(!hs.contains(pair_left[i] + " " + pair_right[i])) {
				hs.add(pair_left[i] + " " + pair_right[i]);
				System.out.println(hs.size());
			}
		}
		if(!hs.contains(pair_left[1] + " " + pair_right[1])) System.out.println("Victory!");
		if(!hs.contains(pair_left[2] + " " + pair_right[2])) System.out.println("Victory 2!");
		
	}
}