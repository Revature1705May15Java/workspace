package com.ex.lambdas;

public class myLambda {
	
	public static void main(String[] args) {
		myLambda math = new myLambda();
		
		MathrOperation addition = (int a, int b) -> a + b;
		MathrOperation subtraction = (int a, int b) -> a - b;
		VarrArgsOps multiply = (int[] x) -> {
			int product = 1;
			for(int i:x) product *= i;
			
			return product;
		};
		int[] nums = {1,2,3,4};
		
		int product = math.operation(multiply, nums);
		System.out.println("Product: " + product);
		
		int sum = math.op(addition, 5, 10);
		System.out.println("Sum = " + sum);
		
		StringTogether combineString = (String a, String b) -> a + b;
		String theString = math.o
	}
	
	private String toOneString(combineString, String a, String b){
		
	};
	
	private int op(MathrOperation up, int a, int b){
		return up.operation(a, b);
	};
	
	private int operation(VarrArgsOps up, int... a){
		return up.operation(a);
	};
	
	
}

interface StringTogether{
	String stringTogether(String a, String b);
}

interface CrazyTruth{
	boolean crazyTruth(int... args);
}

interface MathrOperation{
	int operation(int a, int b);
}

interface VarrArgsOps{
	int operation(int... args);
}